import { QuizcorrectPipe } from './quizcorrect.pipe';

describe('QuizcorrectPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizcorrectPipe();
    expect(pipe).toBeTruthy();
  });
});
